# Optional: full origin of the Nest API (e.g. https://api.example.com).
# Leave unset for local dev: the Vite dev server proxies /api to the backend (see vite.config.js).
# Example when the API runs on another host/port without proxy:
# VITE_API_URL=http://localhost:3001
